- 采用统一的错误处理类 `OrderError` 封装业务异常，并在 API 层通过 `handleApiError` 转换为标准化的 JSON 响应。
- 支付回调处理遵循“验签 -> 幂等性检查 -> 状态机流转 -> 触发履约”的标准流程，确保资金安全与数据一致性。
- 所有涉及金额的计算均使用 `Prisma.Decimal` 或高精度字符串处理，避免浮点数精度丢失问题。
- 系统配置优先从数据库 `SystemConfig` 读取，未配置时回退到环境变量，支持管理后台在线修改即时生效。