- 业务错误统一抛出 `OrderError` 类，携带错误码、多语言消息及 HTTP 状态码，由上层中间件统一处理响应。
- 支付提供商实例配置（如密钥）在数据库中通过 AES 加密存储，读取时调用 `decrypt` 工具函数解密。
- 订单创建与限额校验（如每日累计充值上限）封装在 Prisma 的 `serializable` 事务中，确保并发场景下的数据一致性。
- 所有涉及资金变动的操作（支付、退款、取消）均同步写入 `AuditLog` 表，记录操作人、动作及详细上下文。