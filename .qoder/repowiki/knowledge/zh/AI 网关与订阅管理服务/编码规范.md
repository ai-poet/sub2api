- 服务组件若包含后台协程或定时任务（如 TokenRefreshService, UsageCleanupService），通常在 Wire Provider 中实例化后立即调用 Start() 方法启动。
- Repository 层广泛采用缓存装饰器模式（如 api_key_cache.go, billing_cache.go），在基础 Ent 查询之上封装 Redis 缓存逻辑以提升性能。
- Handler 层通过结构体聚合（如 Handlers, AdminHandlers）统一管理所有 HTTP 处理器，并经由 Wire 注入到路由注册逻辑中。